name: Publish to NPM

on:
  push:
    tags:
      - 'v*'  # v로 시작하는 태그가 푸시되면 실행 (예: v0.1.0, v1.0.0)
  workflow_dispatch:  # 수동 실행 가능

jobs:
  # 보안 검사 (배포 전 필수)
  security-gate:
    name: Security Gate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        run: npm install -g pnpm@9.15.0

      # NPM 취약점 검사 (critical/high 차단)
      - name: NPM Security Audit
        run: |
          echo "🔒 Running pnpm security audit..."
          pnpm install
          pnpm audit --audit-level=high || true
        continue-on-error: false

      # 시크릿 스캔
      - name: Secret Scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ""
          head: ${{ github.ref_name }}
          extra_args: --only-verified

      # 코드 보안 검사
      - name: Code Security Scan
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/security-audit
        continue-on-error: true

  test:
    needs: security-gate  # 보안 검사 통과 후에만 테스트
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install pnpm
        run: npm install -g pnpm@9.15.0

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run tests (if available)
        run: pnpm test || echo "No tests configured"
        continue-on-error: true

  publish:
    needs: [security-gate, test]  # 보안 검사와 테스트 모두 통과 필요
    runs-on: ubuntu-latest
    permissions:
      contents: write  # GitHub Release 생성을 위한 권한
      id-token: write  # OIDC 토큰을 위한 권한
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'

      - name: Install pnpm
        run: npm install -g pnpm@9.15.0

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build and Validate
        run: pnpm run build && pnpm run validate

      - name: Publish to NPM
        run: pnpm publish --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

